# Legacy forward-simulation scripts

Rescued from `Z...CodeIamNotReadyToDelete/` during REFACTOR_PLAN.md Stage 2.
That directory was gitignored, so these files existed in exactly one place on one
disk. Last modified 2026-04-28; they import `ProcessData` and predate several
schema changes, so treat them as reference rather than runnable.
